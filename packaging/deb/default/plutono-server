PLUTONO_USER=plutono

PLUTONO_GROUP=plutono

PLUTONO_HOME=/usr/share/plutono

LOG_DIR=/var/log/plutono

DATA_DIR=/var/lib/plutono

MAX_OPEN_FILES=10000

CONF_DIR=/etc/plutono

CONF_FILE=/etc/plutono/plutono.ini

RESTART_ON_UPGRADE=true

PLUGINS_DIR=/var/lib/plutono/plugins

PROVISIONING_CFG_DIR=/etc/plutono/provisioning

# Only used on systemd systems
PID_FILE_DIR=/var/run/plutono
